import { extendTheme } from "@chakra-ui/react"

export const thema = extendTheme({
  styles: {
    global: {
      body: {
        backgroundColor: "gray.700",
        color: "teal.400",
      }
    }
  }
});
